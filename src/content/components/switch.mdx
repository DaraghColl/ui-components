---
title: 'Switch'
description: 'Simple accessible switch button component'
image: '/component-images/switch.png'
order: 5
---

import Switch from '@ui-components/switch/switch.astro';

<div class="code-example-flex">
	<Switch />
</div>

<div class="markdown-spacer" />

```html title="html"
<label class="switch">
	<span class="switch__name">switch</span>
	<div class="switch__body">
		<input type="checkbox" class="switch__checkbox" />
		<span class="switch__ball"></span>
	</div>
</label>
```

```css title="css"
.switch {
	display: flex;
	align-items: center;
	gap: var(--spacing-sm);
	width: fit-content;
	cursor: pointer;
}

.switch__body {
	display: flex;
	align-items: center;
	justify-content: flex-start;
	background-color: var(--color-bg);
	width: 4rem;
	height: 2rem;
	padding: var(--spacing-xs);
	border: var(--border-sm) solid var(--border-color-default);
	border-radius: var(--rounded-2xl);
	box-shadow: var(--shadow);
}

.switch__body:focus-visible {
	outline: var(--border-md) solid var(--color-accent);
}

.switch__body:disabled {
	background-color: var(--color-font-secondary);
}

.switch__body:disabled .switch__ball {
	background-color: var(--color-font);
}

.switch__body:has(input:checked) {
	background-color: var(--color-accent-lighten);
}

.switch__checkbox {
	opacity: 0;
	width: 0;
	height: 0;
}

.switch__checkbox:checked + .switch__ball {
	background-color: var(--color-accent);
	transform: translateX(1.9rem);
	transition: var(--transition-short) ease-in;
}

.switch__ball {
	display: block;
	background-color: var(--color-accent);
	width: 1.5rem;
	height: 1.5rem;
	border-radius: var(--rounded-full);
	transition: var(--transition-short) ease-in;
}
```
