<div class="landing">
	<div class="landing__content">
		<h1 class="landing__heading">Explore Donegal</h1>
		<p class="landing__description">
			See all the sites in this beatiful county - Errigal, Slieve League, Glenveagh National Park
			and more
		</p>
		<button class="landing__button">See More</button>
	</div>
	<img src="/donegal.jpg" class="landing__image" />
</div>

<style>
	.landing {
		display: flex;
		flex-direction: column-reverse;
		align-items: center;
		gap: var(--spacing-lg);
	}

	.landing__content {
		display: flex;
		flex-direction: column;
		gap: var(--spacing-lg);
		width: 100%;
	}

	.landing__description {
		color: var(--color-font-secondary);
		font-size: 0.8rem;
	}

	.landing__button {
		display: flex;
		align-self: self-start;
		gap: var(--spacing-md);
		background-color: var(--color-accent);
		color: white;
		padding: var(--spacing-md);
		border: var(--border-sm) solid var(--color-accent);
		border-radius: var(--rounded-sm);
		box-shadow: var(--shadow);
		font-size: var(--font-md);
		cursor: pointer;
	}

	.landing__button:hover {
		background-color: var(--color-accent-darken);
	}

	.landing__button:focus-visible {
		outline: 2px solid var(--color-accent);
	}

	.landing__image {
		width: 100%;
		object-fit: cover;
		border-radius: var(--rounded-md);
	}

	@media (min-width: 768px) {
		.landing {
			flex-direction: row;
		}

		.landing__content {
			width: 50%;
		}

		.landing__image {
			width: 50%;
		}
	}
</style>
